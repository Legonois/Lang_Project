---
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<!-- header for my website using tailwind -->
		<header class="bg-white shadow" id="header">
			<div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
				<h1 class="text-3xl font-bold text-gray-900">{title}</h1>

				<!-- navigation items -->
				<nav class="flex space-x-4 mt-4">
					<a href="/" class="text-sm font-medium text-gray-500 hover:text-gray-700">Home</a>
					<a href="/movie" class="text-sm font-medium text-gray-500 hover:text-gray-700">Movie</a>
					<a href="/america" class="text-sm font-medium text-gray-500 hover:text-gray-700">The Corruption of the American Dream</a>
					<a href="/characters" class="text-sm font-medium text-gray-500 hover:text-gray-700">Characters</a>
				</nav>
			</div>
		</header>
		<slot />
		<footer>
			<div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 border-t-2 bg-gray-150">
				<p class="text-center text-base text-gray-400">
					&copy; 2023 <a href="https://www.ManyBees.net" class="hover:text-gray-500">Wesley Baldwin</a>, and <a href="http://www.LunarHUE.com" class="hover:text-gray-500">LunarHUE</a>
				</p>
			</div>
		</footer>
	</body>
</html>
<style is:global>
	:root {
		--accent: 124, 58, 237;
		--accent-gradient: linear-gradient(45deg, rgb(var(--accent)), #da62c4 30%, white 60%);
	}
	html {
		font-family: system-ui, sans-serif;
		background-color: #F6F6F6;
	}
	code {
		font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono,
			Bitstream Vera Sans Mono, Courier New, monospace;
	}
	header.transparent {
  		background-color: transparent;
	}

</style>

<script>
	// const header = document.querySelector('#header');

	// if (header) {
	// window.addEventListener('scroll', () => {
	// 	if (window.scrollY > 0) {
	// 	header.classList.add('transparent');
	// 	} else {
	// 	header.classList.remove('transparent');
	// 	}
	// });
	// }
</script>
  